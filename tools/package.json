{
  "name": "donde-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Offline build pipeline that turns public datasets into the app's bundled reference.db and map geometry. Kept out of the app package so its native deps never reach Metro.",
  "scripts": {
    "build:db": "tsx src/build-db.ts",
    "build:geometry": "tsx src/build-geometry.ts",
    "build": "npm run build:db && npm run build:geometry"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "better-sqlite3": "^12.2.0",
    "mapshaper": "^0.6.109"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.7",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.3.0",
    "tsx": "^4.20.4",
    "typescript": "^5.9.2"
  }
}
